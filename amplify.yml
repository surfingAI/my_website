version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No pre-build required"
    build:
      commands:
        - echo "No build required"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: [] 